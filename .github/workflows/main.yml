name: CI

on:
  push:
    branches:
      - '*'
#    tags:
#      - '!v*'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Setup JDK 17
        uses: actions/setup-java@v2
        with:
          distribution: 'temurin'
          java-version: '17'
          cache: 'gradle'

      - name: Environment info
        run: |
          gradle --version

      - name: build
        run: |
          ./gradlew assembleComoresDevDebug --stacktrace

      - name: Environment info
        run: |
          gradle --version

      - uses: actions/upload-artifact@v4
        with:
          name: openimis-policies-apk-${{github.run_number}}-${{github.sha}}.apk
          path: ./app/build/outputs/**/*.apk

      - name: build
        run: |
          ./gradlew bundleComoresDevDebug --stacktrace

      - name: Environment info
        run: |
          gradle --version

      - uses: actions/upload-artifact@v4
        with:
          name: openimis-policies-aab-${{github.run_number}}-${{github.sha}}.aab
          path: ./app/build/outputs/**/*.aab
